{
  "name": "@workspace/three-js-50",
  "version": "1.0.0",
  "dependencies": {
    "parcel": "latest"
  },
  "scripts": {
    "clean": "rm -rf dist .parcel-cache",
    "build": "node ./node_modules/.bin/parcel build --no-optimize --no-cache",
    "build:optimize": "node ./node_modules/.bin/parcel build --no-cache"
  },
  "targets": {
    "default": {
      "source": "./src/index.js",
      "distDir": "./dist",
      "sourceMap": false
    }
  }
}
